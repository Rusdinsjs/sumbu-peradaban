super.sjsgroup.site {
    # ── Sumbu Peradaban Reverse Proxy ──

    # Route GraphQL API requests to Backend
    handle /graphql* {
        reverse_proxy backend:8080
    }
    
    # Route all other requests to SvelteKit Frontend
    handle /* {
        reverse_proxy frontend:5173
    }
}
